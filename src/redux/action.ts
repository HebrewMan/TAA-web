import { SET_POPUS_STATUS } from "./type"

export const setPopusStatus = (data:any) => ({type:SET_POPUS_STATUS,data})

// export const changeStatus = (status:any) => ({ type: 'change-status', key: status })
